#include "common/stl/base.h"

int main_swordmaster() {
  unsigned T;
  cin >> T;
  for (unsigned it = 1; it <= T; ++it) {
    unsigned N, P;
    cin >> N >> P;
    for (unsigned in = 0; in < N; ++in) {
    }
    cout << "Case #" << it << ": " << 0 << endl;
  }
  return 0;
}
