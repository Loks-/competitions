#include "common/stl/base.h"

#include <string>

int main_two_tiling() {
  unsigned T;
  cin >> T;
  for (unsigned it = 1; it <= T; ++it) {
    // string s;
    // vector<string> vs;
    // for (; vs.size() < 3;)
    cout << "Case #" << it << ": " << 0 << endl;
  }
  return 0;
}
