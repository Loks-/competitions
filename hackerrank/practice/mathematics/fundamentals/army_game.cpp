// https://www.hackerrank.com/challenges/game-with-cells

#include "common/stl_base.h"

int main_army_game()
{
	uint64_t n, m;
	cin >> n >> m;
	cout << ((n + 1) / 2) * ((m + 1) / 2) << endl;
	return 0;
}
